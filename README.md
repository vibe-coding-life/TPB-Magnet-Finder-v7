# TPB-Magnet-Finder-v7
A Python tool that searches a list of movie queries, finds suitable magnet links, and saves them to a clean output file. It includes a validated rotating proxy pool with HTTP/SOCKS5 support, multithreaded proxy testing, duplicate removal, Colorama status output, and separate logging of failed searches.
